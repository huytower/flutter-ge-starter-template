enum HttpStatus { loading, completed, error, clientError, serviceError, redirection }

enum LayoutStatus {
  loading,
  load_more,
  success,
  empty,
  error,
}

enum Events {
  processing,
  openLoginPage,
  none,
  error_1,
  error_2,
  error_3,
  error_4,
}
