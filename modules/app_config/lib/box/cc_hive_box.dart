/// Define Keys to store|get in HiveBox (Shared Prefs.)
class CcHiveBox {
  // key name.
  // static const cc_key = 'aff_seller_info_key';
  // static const key_list_init = 'key_list_init';
  static const key_default = "key_default";

  // type id, using for multi-prefs.
  static const test_type_id = 1;
  static const application_type_id = 2;
  static const device_type_id = 3;
  static const app_track_log_type_id = 4;

  // hive box name, using for multi-prefs.
  static const application_box_name = 'application';
  static const device_info_box_name = 'device';
  static const app_track_log_box_name = 'track_log';

// end.
}
