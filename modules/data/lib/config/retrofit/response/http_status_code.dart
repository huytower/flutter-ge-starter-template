// class HttpStatusCode {
//   static var info = List.generate(99, (index) => 100 + index);
//   static var success = List.generate(99, (index) => 200 + index);
//   static var clientError = List.generate(99, (index) => 400 + index);
//   static var serviceError = List.generate(99, (index) => 500 + index);
//   static var redirection = List.generate(99, (index) => 300 + index);
// }
